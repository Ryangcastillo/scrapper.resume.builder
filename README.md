# Penny
├── README.md
├── .env.example
├── package.json
├── pages/
│   index.js, upload.js, jobs.js, cv-editor.js
├── pages/api/
│   db.js, scrape.js, parse-cv.js, match.js, export-cv.js
├── lib/db.js
└── components/
    NavBar.js, JobCard.js, Heatmap.js, CVEditor.js

    You are my expert AI developer assistant. I’m building **CiVi**, an AI‑powered job‑matching web app for New Zealanders. I am **not a programmer**, so please guide me **step‑by‑step** in Tasks‑Master style with Epics and Ticket numbers. Leave a short progress note before closing each ticket.

---

🏗️ **App Overview:**  
- Scrapes NZ‑only job sites (Seek, Trade Me Jobs, Indeed NZ)  
- Users upload their **CV**  
- Analyzes it against job listings  
- Scores each job match (%)  
- AI‑suggested CV improvements  
- Enables **CV export** (PDF/Word) using NZ templates  
- Includes **Skill‑Gap Advisor** with skill demand % and training links  

---

⚙️ **Tech Stack:**  
- Frontend/API: `Next.js`  
- Database: Oracle Autonomous Database  
- AI: OpenAI or GPT  
- CV parsing & export: `pdf-parse`, `html-pdf`, `docx`  
- UI: Tailwind or Chakra  

---

🧱 **Database Schema:**

```sql
-- job_opening_database
CREATE TABLE job_postings (
  job_code VARCHAR2(50) PRIMARY KEY,
  company_name VARCHAR2(100),
  job_title VARCHAR2(100),
  job_description CLOB,
  salary_range VARCHAR2(100),
  industry_category VARCHAR2(100),
  industry_sub_category VARCHAR2(100),
  location VARCHAR2(100),
  posting_date DATE,
  closing_date DATE
);
CREATE TABLE job_requirements (
  id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  job_code VARCHAR2(50),
  requirement_text VARCHAR2(500),
  FOREIGN KEY(job_code) REFERENCES job_postings(job_code)
);
CREATE TABLE job_skills (
  id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  job_code VARCHAR2(50),
  skill_text VARCHAR2(200),
  FOREIGN KEY(job_code) REFERENCES job_postings(job_code)
);

-- user_database
CREATE TABLE user_profiles (
  user_code VARCHAR2(50) PRIMARY KEY,
  full_name VARCHAR2(100),
  email VARCHAR2(100),
  education_summary VARCHAR2(300),
  experience_summary CLOB,
  uploaded_cv_url VARCHAR2(200)
);
CREATE TABLE user_requirements (
  id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_code VARCHAR2(50),
  requirement_text VARCHAR2(500),
  FOREIGN KEY(user_code) REFERENCES user_profiles(user_code)
);
CREATE TABLE user_skills (
  id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_code VARCHAR2(50),
  skill_text VARCHAR2(200),
  FOREIGN KEY(user_code) REFERENCES user_profiles(user_code)
);

npm install oracledb multer pdf-parse html-pdf docx openai

🎯 Core Features + AI Enhancements:

Scrape NZ jobs, parse CVs, compute match %, filter & sort jobs

AI Tailored CV Rewrite Suggestions (NZ style bullets, quantifiable metrics)

Export selectable Kiwi CV templates as PDF or Word

Skill‑Gap Advisor: identifies missing skills, shows demand %, links to free NZ training

Heatmap: visual skill alignment

Save multiple tailored CV versions per job

Generate a short cover letter or “Why this job fits” section

Data privacy control & deletion

✅ Epics & Tickets (Tasks Master):

Epic 1: Oracle Database Setup
– 1.1: Connect to Oracle from Next.js
– 1.2: Create all tables
– 1.3: Seed mock job & CV data

Epic 2: Job Scraper
– 2.1: Scrape NZ sites
– 2.2: Insert into job_postings
– 2.3: Parse bullets into job_requirements/job_skills

Epic 3: CV Upload & Parsing
– 3.1: Upload UI
– 3.2: Parse CV
– 3.3: Save parsed data

Epic 4: Matching & Display
– 4.1: Compute match score
– 4.2: Filter & sort
– 4.3: Job cards with match % & heatmap

Epic 5: AI Tailoring
– 5.1: Identify missing skills
– 5.2: Suggest bullet improvements
– 5.3: Save updated CV version

Epic 6: Export & Templates
– 6.1: Show free Kiwi templates
– 6.2: Apply edits to template
– 6.3: Export and download PDF / Word

Epic 7: Skill‑Gap Advisor
– 7.1: Fetch NZ skill demand %
– 7.2: Offer training links
– 7.3: “Add to CV” suggestions

Epic 8: Bonus UX Features
– 8.1: CV history view per job
– 8.2: Cover letter generator
– 8.3: Privacy mode & data wipe

📊 Skill‑Gap Advisor Example:

Compare user_skills to job_skills

Show missing: e.g., “Cloud computing” – required in 65% of Kiwi job ads

Link to free training (e.g., Coursera’s free AWS course)

Suggest adding to CV to improve match by X%

📝 UI Flow (ASCII):

csharp
Copy
Edit
[Landing / Upload CV]
       ↓
[Parse CV → Store in DB]
       ↓
[Job Listings w/ filters & match %]
       ↓
[Job Detail]
   ↳ Heatmap & missing skills
   ↳ AI CV improvement
       ↓
[Apply edits → Export CV (choose template) → Download]

## New Initiatives Roadmap

Refer to [`docs/tasks-master.md`](docs/tasks-master.md) for Epics 9–12 covering testing, performance, email integration, and Vercel deployment pipeline workstreams.
